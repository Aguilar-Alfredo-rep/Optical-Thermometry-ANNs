# Optical-Thermometry-ANNs
Implementation of neural networks in TensorFlow (Python) to predict temperatures via luminescence thermometry using upconversion nanoparticles. It features convolutional models with physics-informed attention and feedforward architectures, achieving sub-0.3°C RMSE on independent spectra.
